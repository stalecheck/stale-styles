{
  "name": "@stale-styles/cli",
  "version": "1.0.1",
  "description": "Executable CLI for @stale-styles/css-modules.",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/stalecheck/stale-styles.git",
    "directory": "packages/cli"
  },
  "bin": {
    "stale-styles": "./dist/cli.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "type": "module",
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org/"
  },
  "scripts": {
    "build": "tsup --config ./tsup.config.ts",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "test": "vitest run --root ../.. --project cli",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@stale-styles/css-modules": "workspace:*",
    "commander": "^15.0.0"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
